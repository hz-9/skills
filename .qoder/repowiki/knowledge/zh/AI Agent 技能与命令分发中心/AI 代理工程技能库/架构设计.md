- **模块化技能封装**：每个子目录（如 `tdd`, `diagnose`）代表一个独立的原子技能，通过 `SKILL.md` 定义触发条件、执行流程与输出规范。
- **共享知识依赖**：技能间通过引用共享文档（如 `LANGUAGE.md` 定义架构术语，`AGENT-BRIEF.md` 规范代理指令）保持语义一致性，避免重复定义。
- **配置引导机制**：`setup-matt-pocock-skills` 作为前置引导技能，负责初始化仓库级的 Issue 跟踪器映射、Triage 标签词汇表及领域文档路径，确保其他技能在正确的上下文中运行。
- **文档驱动决策**：深度集成 `CONTEXT.md`（领域术语表）和 `ADR`（架构决策记录），要求技能在执行过程中实时校验并更新这些文档，实现代码与文档的同步演进。