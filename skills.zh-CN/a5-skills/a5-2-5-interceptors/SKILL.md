---
name: nestjs-2-5-interceptors
description: 审查 NestJS 拦截器的设计与实现，涵盖请求/响应转换、日志记录、缓存和异常映射等切面逻辑。当用户需要审查或开发拦截器逻辑时使用。
---

# NestJS 切面拦截与数据处理

## Overview

当 AI 在 NestJS 项目中遇到拦截器（Interceptor）相关代码时，自动执行以下工作：审查拦截器的实现和注册方式，检查 RxJS 操作符的使用规范性，评估响应映射和数据转换逻辑的正确性，识别常见的拦截器陷阱，并提供改进建议。

## Definitions

- <a id="目标代码"></a>**目标代码**：当前对话中 NestJS 的拦截器实现代码或使用拦截器的控制器代码。
- <a id="拦截器接口"></a>**拦截器接口**：所有拦截器需实现的 NestInterceptor 接口，包含 intercept(context: ExecutionContext, next: CallHandler) 方法。
- <a id="CallHandler"></a>**CallHandler**：拦截器中用于调用下一个处理程序的接口，通过 handle() 方法返回 Observable。
- <a id="RxJS 操作符"></a>**RxJS 操作符**：拦截器中常用的 RxJS 操作符，包括 map、tap、catchError、timeout、retry 等。
- <a id="是否分析完成"></a>**是否分析完成**：标记对目标代码的分析是否已得出完整结果。

## Prerequisites

- NestJS 项目环境；
- 拦截器实现代码可访问；
- 了解 RxJS 基本操作符和 Observable 模式。

## Workflow

0. **前置检查** — 确保目标代码和运行环境可达；
   - 判断目标代码是否存在且可读取：
     - 是 -> 下一步；
     - 否 -> 提示用户提供目标代码或文件路径，阻塞等待用户输入；
   - 初始化全局变量 [是否分析完成](#是否分析完成)：
     - 判断代码是否完整可解析：
       - 满足 -> 初始化变量为 true；
       - 不满足 -> 初始化变量为 false；

1. **分析拦截器代码** — 读取并理解拦截器实现和数据处理逻辑；
   - 读取目标代码，识别以下核心要素：
     - 拦截器的实现方式（实现 NestInterceptor 接口）；
     - intercept 方法中调用 next.handle() 的方式；
     - pipe() 中 RxJS 操作符链的顺序（map、tap、catchError 等）；
     - 拦截器的注册方式（全局 / 控制器 / 方法级）；

2. **逐项审查** — 对照审查清单检查拦截器代码质量；
   - 依次判断以下审查项是否通过：
     - 拦截器是否调用了 next.handle()（不调用则下游控制器不会执行）：
       - 通过 -> 记录通过，继续下一个审查项；
       - 未通过 -> 记录“未调用 next.handle()，控制器方法不会被执行”，继续；
     - RxJS pipe() 中是否包含 catchError 操作符（避免异常未被捕获传播到客户端）：
       - 通过 -> 记录通过，继续下一个审查项；
       - 未通过 -> 记录“缺少 catchError，拦截器中抛出的异常可能无法正确处理”，继续；
     - 响应映射逻辑是否正确（map 操作符中的转换函数是否保持了数据结构完整性）：
       - 通过 -> 记录通过，继续下一个审查项；
       - 未通过 -> 记录“响应映射可能改变了预期的响应结构”，继续；
     - 拦截器是否使用了 Observable 操作符链而非原始 Promise（充分利用 RxJS 能力）：
       - 通过 -> 记录通过，继续下一个审查项；
       - 未通过 -> 记录“建议使用 RxJS 操作符链替代原始 Promise 以利用操作符能力”，继续；
     - 超时拦截器中 timeout 操作符是否设置了合适的值（避免误杀正常请求）：
       - 通过 -> 记录通过，继续下一个审查项；
       - 未通过 -> 记录“超时时间设置不合理，建议根据业务场景调整”，继续；
     - 多个拦截器的执行顺序是否符合预期（全局→控制器→方法）：
       - 通过 -> 记录通过，继续下一个审查项；
       - 未通过 -> 记录“拦截器注册顺序可能导致意外的行为，请检查执行顺序”，继续；
   - 判断是否有任何问题记录：
     - 是 -> 汇总问题列表，进入下一步；
     - 否 -> 直接进入步骤 4（复核检查）；

3. **提供修改建议** — 对发现的问题给出具体修复方案；
   - 依次对每个问题提供修复建议；
   - 通过 AskUserQuestion 提供选项，阻塞等待用户选择：
     - 采纳全部建议 -> 生成修正后的代码，进入步骤 4；
     - 逐条确认 -> 逐条由用户决定采纳或忽略，全部确认后进入步骤 4；
     - 仅查看不修改 -> 不影响代码，直接进入步骤 4；

4. **复核检查** — 对照 [Review List](#review-list)，确认执行结果；
   - 判断 Review List 是否有内容：
     - 否 -> 直接进入下一步（成果输出）；
     - 是 -> 下一步；
   - 依次判断 [Review List](#review-list) 中每个检查项，是否通过：
     - 基于“复核检查示例”来显示输出内容；
   - 有任一检查失败，终止流程；
   - 全部通过后，进入下一步；

5. **成果输出** — 输出审查摘要，告知完成；
   - 输出结构化摘要表格（审查文件 / 审查项数 / 通过 / 发现问题 / 风险等级 / 已采纳建议）；
   - 告知审查完成；

## Rules

- **内容规范**
  - description 须遵循格式：第一句说明审查内容，第二句说明触发条件，使用第三人称，不超过 1024 字符；
  - 审查项须引用 NestJS 官方关于拦截器的文档；
  - SKILL.md 不超过 300 行；超过时拆分到 references/ 目录；
  - 引用层次不超过一层；
  - 保持术语一致，Definitions 中已定义术语在正文中通过锚点链接引用；
  - 删除任何时效性信息；

- **行为规范**
  - 审查时仅输出问题摘要和定位，不直接修改代码，除非用户通过 AskUserQuestion 明确要求；
  - 所有涉及代码修改的交互环节，必须使用 AskUserQuestion 工具；
  - RxJS 操作符的选择标注为建议而非强制（不同场景适合不同操作符）；

- **防御规范**
  - 如目标代码为空或不可读，直接报告并终止；
  - 超时时间合理性仅在其他同类接口可参考时提供建议，否则标注为需手动确认；

- **验证规范**
  - Examples 内容必须自洽于 Rules；
  - Examples 必须包含复核检查示例；
  - 对话交互示例仅聚焦步骤 0~3（不含复核检查和成果输出）；

## Examples

### 对话交互示例

**示例：用户请求审查拦截器实现**

```markdown
用户 > 帮我检查这个响应包装拦截器
AI   > 检测到用户需要 NestJS 拦截器审查，触发 nestjs-2-5-interceptors 技能
AI   > 正在分析拦截器代码...

审查结果：
- 🟩 正确实现了 NestInterceptor 接口
- 🟩 调用了 next.handle() 确保控制器正常执行
- 🟥 pipe() 中缺少 catchError，异常未被捕获
- 🟥 使用了 Promise.then() 而非 RxJS map 操作符
- 🟩 注册在全局级别，作用于所有路由

总评：2 个问题需要关注
用户 > 请帮我把 Promise 替换为 RxJS 操作符
AI   > 可以使用 pipe(map()) 替代 .then()。是否应用修改？
用户 > 全部采纳
```

### 复核检查示例

```markdown
AI > 进入复核检查，Review List 包含 6 个检查项，开始逐项验收：

**内容检查**
  - 🟩 审查项全部引用官方拦截器文档
  - 🟩 RxJS 操作符建议标注为建议

**行为检查**
  - 🟩 未直接修改用户代码
  - 🟩 使用了 AskUserQuestion

**验证检查**
  - 🟩 超时建议标注为需手动确认
  - 🟩 输出摘要完整

✅ 全部通过，进入成果输出。
```

### 成果输出示例

**审查结果示例：**

```markdown
| 维度 | 结果 |
| --- | --- |
| 审查文件 | src/common/interceptors/response-wrapper.interceptor.ts |
| 审查项总数 | 7 项 |
| 通过 | 5 项 |
| 发现问题 | 2 项 |
| 风险等级 | 🟡 中 |
| 已采纳建议 | 2 条 |
```

## Review List

- **内容检查**
  - [ ] 审查项全部引用 NestJS 官方拦截器文档
  - [ ] RxJS 操作符选择标注为建议而非强制
- **行为检查**
  - [ ] 未直接修改用户代码（除非用户明确要求）
  - [ ] 所有交互环节使用了 AskUserQuestion
- **验证检查**
  - [ ] 目标代码为空或不可读时已正确终止
  - [ ] 超时时间合理性标注为需手动确认
  - [ ] 输出摘要包含了文件路径、审查项数、发现问题和风险等级

## References

- [NestJS 拦截器官方文档](https://docs.nestjs.com/interceptors)
- [RxJS 操作符文档](https://rxjs.dev/api/operators)
- [skill-evolve 模板](../../skill-evolve/template.md)
