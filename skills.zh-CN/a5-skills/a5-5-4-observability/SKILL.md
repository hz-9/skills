---
name: nestjs-5-4-observability
description: 审查 NestJS 系统可观测性实现，涵盖日志记录、指标收集和分布式追踪的最佳实践。当用户需要审查可观测性配置或排查监控问题时使用。
---

# NestJS 系统可观测性

## Overview

当 AI 在 NestJS 项目中遇到日志、监控或追踪相关代码时，自动执行以下工作：审查日志记录策略（内置 Logger vs 第三方集成），检查健康检查端点的配置，评估指标收集和分布式追踪的实现，并提供改进建议。

## Definitions

- <a id="目标代码"></a>**目标代码**：当前对话中 NestJS 项目的日志配置、健康检查或监控相关代码。
- <a id="健康检查"></a>**健康检查**：通过 @nestjs/terminus 实现的健康检查端点，用于监控数据库、外部服务和系统资源的状态。
- <a id="Logger"></a>**Logger**：NestJS 内置的 Logger 接口，可自定义实现或集成 Winston、Pino 等日志库。
- <a id="是否分析完成"></a>**是否分析完成**：标记对目标代码的分析是否已得出完整结果。

## Prerequisites

- NestJS 项目环境；
- 日志配置、健康检查或监控相关代码可访问；
- 了解可观测性的三大支柱（日志、指标、追踪）。

## Workflow

0. **前置检查** — 确保目标代码和运行环境可达；
   - 判断目标代码是否存在且可读取：
     - 是 -> 下一步；
     - 否 -> 提示用户提供目标代码或文件路径，阻塞等待用户输入；
   - 初始化全局变量 [是否分析完成](#是否分析完成)：
     - 判断代码是否完整可解析：
       - 满足 -> 初始化变量为 true；
       - 不满足 -> 初始化变量为 false；

1. **分析可观测性代码** — 读取并理解日志、监控和追踪实现；
   - 读取目标代码，识别以下核心要素：
     - Logger 的使用方式（内置 console.log vs 自定义 Logger 服务）；
     - 是否集成了第三方日志库（Winston、Pino 等）；
     - 健康检查端点的配置（TerminusModule.forRoot）；
     - 请求日志拦截器（记录请求方法、路径、响应时间）；

2. **逐项审查** — 对照审查清单检查可观测性代码质量；
   - 依次判断以下审查项是否通过：
     - 是否使用了 NestJS 的 Logger 接口而非直接 console.log（便于后续替换和统一管理）：
       - 通过 -> 记录通过，继续下一个审查项；
       - 未通过 -> 记录“使用 console.log 记录日志，建议替换为 Logger 接口”，继续；
     - 生产环境是否配置了日志级别（避免调试日志泄漏到生产环境）：
       - 通过 -> 记录通过，继续下一个审查项；
       - 未通过 -> 记录“缺少日志级别控制，生产环境可能输出过多调试日志”，继续；
     - 是否配置了健康检查端点（通过 @nestjs/terminus 监控关键依赖状态）：
       - 通过 -> 记录通过，继续下一个审查项；
       - 未通过 -> 记录“缺少健康检查端点，建议添加 @nestjs/terminus”，继续；
     - 请求日志拦截器是否记录了关键信息（请求方法、路径、状态码、响应时间）：
       - 通过 -> 记录通过，继续下一个审查项；
       - 未通过 -> 记录“请求日志缺少响应时间等关键指标，建议添加拦截器记录”，继续；
     - 是否将日志结构化（JSON 格式）而非自由文本（便于日志聚合系统解析）：
       - 通过 -> 记录通过，继续下一个审查项；
       - 未通过 -> 记录“建议使用 JSON 格式记录日志以便日志系统解析”，继续；
   - 判断是否有任何问题记录：
     - 是 -> 汇总问题列表，进入下一步；
     - 否 -> 直接进入步骤 4（复核检查）；

3. **提供修改建议** — 对发现的问题给出具体修复方案；
   - 依次对每个问题提供修复建议；
   - 通过 AskUserQuestion 提供选项，阻塞等待用户选择：
     - 采纳全部建议 -> 生成修正后的代码，进入步骤 4；
     - 逐条确认 -> 逐条由用户决定采纳或忽略，全部确认后进入步骤 4；
     - 仅查看不修改 -> 不影响代码，直接进入步骤 4；

4. **复核检查** — 对照 [Review List](#review-list)，确认执行结果；
   - 判断 Review List 是否有内容：
     - 否 -> 直接进入下一步（成果输出）；
     - 是 -> 下一步；
   - 依次判断 [Review List](#review-list) 中每个检查项，是否通过：
     - 基于“复核检查示例”来显示输出内容；
   - 有任一检查失败，终止流程；
   - 全部通过后，进入下一步；

5. **成果输出** — 输出审查摘要，告知完成；
   - 输出结构化摘要表格（审查文件 / 审查项数 / 通过 / 发现问题 / 风险等级 / 已采纳建议）；
   - 告知审查完成；

## Rules

- **内容规范**
  - description 须遵循格式：第一句说明审查内容，第二句说明触发条件，使用第三人称，不超过 1024 字符；
  - 审查项须引用 NestJS 官方文档和日志最佳实践；
  - SKILL.md 不超过 300 行；超过时拆分到 references/ 目录；
  - 引用层次不超过一层；
  - 保持术语一致，Definitions 中已定义术语在正文中通过锚点链接引用；
  - 删除任何时效性信息；

- **行为规范**
  - 审查时仅输出问题摘要和定位，不直接修改代码，除非用户通过 AskUserQuestion 明确要求；
  - 所有涉及代码修改的交互环节，必须使用 AskUserQuestion 工具；
  - 第三方日志库的选择（Winston / Pino）标注为技术选型而非缺陷；

- **防御规范**
  - 如目标代码为空或不可读，直接报告并终止；
  - 日志级别控制建议仅在存在条件判断或配置时提供，无基准配置时不强行要求；

- **验证规范**
  - Examples 内容必须自洽于 Rules；
  - Examples 必须包含复核检查示例；
  - 对话交互示例仅聚焦步骤 0~3（不含复核检查和成果输出）；

## Examples

### 对话交互示例

**示例：用户请求审查可观测性配置**

```markdown
用户 > 帮我检查项目的日志和监控配置
AI   > 检测到用户需要 NestJS 可观测性审查，触发 nestjs-5-4-observability 技能
AI   > 正在分析日志和监控代码...

审查结果：
- 🟥 控制器中直接使用 console.log，建议使用 Logger 接口
- 🟥 生产环境未配置日志级别
- 🟩 @nestjs/terminus 健康检查已配置
- 🟥 缺少请求日志拦截器（响应时间等指标）
- 🟩 日志使用 JSON 格式结构化输出

总评：3 个问题需要关注
用户 > 帮我添加 Logger 和请求日志拦截器
AI   > 创建 LoggerService 替换 console.log，添加 LoggingInterceptor。是否应用？
用户 > 全部采纳
```

### 复核检查示例

```markdown
AI > 进入复核检查，Review List 包含 5 个检查项，开始逐项验收：

**内容检查**
  - 🟩 审查项全部引用官方文档
  - 🟩 日志库选择标注为技术选型

**行为检查**
  - 🟩 未直接修改用户代码
  - 🟩 使用了 AskUserQuestion

**验证检查**
  - 🟩 日志级别在可配置时建议
  - 🟩 输出摘要完整

✅ 全部通过，进入成果输出。
```

### 成果输出示例

**审查结果示例：**

```markdown
| 维度 | 结果 |
| --- | --- |
| 审查文件 | src/app.module.ts、src/user/user.controller.ts |
| 审查项总数 | 7 项 |
| 通过 | 4 项 |
| 发现问题 | 3 项 |
| 风险等级 | 🟡 中 |
| 已采纳建议 | 2 条 |
| 已忽略/仅查看 | 1 条 |
```

## Review List

- **内容检查**
  - [ ] 审查项全部引用 NestJS 官方日志和健康检查文档
  - [ ] 第三方日志库选择标注为技术选型
- **行为检查**
  - [ ] 未直接修改用户代码（除非用户明确要求）
  - [ ] 所有交互环节使用了 AskUserQuestion
- **验证检查**
  - [ ] 目标代码为空或不可读时已正确终止
  - [ ] 日志级别建议在可配置时提供
  - [ ] 输出摘要包含了文件路径、审查项数、发现问题和风险等级

## References

- [NestJS 日志官方文档](https://docs.nestjs.com/techniques/logger)
- [NestJS 健康检查](https://docs.nestjs.com/recipes/terminus)
- [NestJS 性能监控](https://docs.nestjs.com/techniques/performance)
- [skill-evolve 模板](../../skill-evolve/template.md)
