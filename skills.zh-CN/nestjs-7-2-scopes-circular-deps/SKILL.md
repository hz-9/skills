---
name: nestjs-7-2-scopes-circular-deps
description: 审查 NestJS 注入作用域和循环依赖的配置与管理，涵盖 REQUEST/TRANSIENT 作用域和 forwardRef 使用。当用户需要排查注入作用域问题或循环依赖时使用。
---

# NestJS 作用域与循环依赖

## Overview

当 AI 在 NestJS 项目中遇到注入作用域或循环依赖相关代码时，自动执行以下工作：审查作用域配置是否合理，检查循环依赖的存在并提供 forwardRef 解决方案，评估作用域对性能的影响，并提供改进建议。

## Definitions

- <a id="目标代码"></a>**目标代码**：当前对话中 NestJS 的 @Injectable scope 配置或存在循环依赖的模块或服务代码。
- <a id="注入作用域"></a>**注入作用域**：NestJS 提供的作用域类型 —— DEFAULT（单例）、REQUEST（请求级）、TRANSIENT（注入级）。
- <a id="forwardRef"></a>**forwardRef**：用于解决循环依赖的辅助函数，允许在依赖关系未完全建立时引用对方。
- <a id="是否分析完成"></a>**是否分析完成**：标记对目标代码的分析是否已得出完整结果。

## Prerequisites

- NestJS 项目环境；
- 涉及作用域或循环依赖的代码文件可访问；
- 了解 DI 机制和模块加载顺序的基本概念。

## Workflow

0. **前置检查** — 确保目标代码和运行环境可达；
   - 判断目标代码是否存在且可读取：
     - 是 -> 下一步；
     - 否 -> 提示用户提供目标代码或文件路径，阻塞等待用户输入；
   - 初始化全局变量 [是否分析完成](#是否分析完成)：
     - 判断代码是否完整可解析：
       - 满足 -> 初始化变量为 true；
       - 不满足 -> 初始化变量为 false；

1. **分析代码** — 读取并理解作用域配置和依赖关系；
   - 读取目标代码，识别以下核心要素：
     - @Injectable 或 @Controller 上的 scope 配置；
     - 模块间的 import 链条是否存在循环；
     - 服务间构造函数注入是否存在相互引用；

2. **逐项审查** — 检查作用域和循环依赖问题；
   - 依次判断以下审查项是否通过：
     - 是否不必要地使用了 REQUEST 作用域（默认 SINGLETON 应为首选）：
       - 通过 -> 记录通过，继续下一个审查项；
       - 未通过 -> 记录“REQUEST 作用域每次请求创建新实例，可能影响性能”，继续；
     - 循环依赖是否使用了 forwardRef 解决：
       - 通过 -> 记录通过，继续下一个审查项；
       - 未通过 -> 记录“检测到循环依赖缺少 forwardRef，建议添加”，继续；
     - TRANSIENT 作用域的使用是否确实需要每次注入都创建新实例：
       - 通过 -> 记录通过，继续下一个审查项；
       - 未通过 -> 记录“TRANSIENT 作用域可能导致大量实例创建，请确认必要性”，继续；
   - 判断是否有任何问题记录：
     - 是 -> 汇总问题列表，进入下一步；
     - 否 -> 直接进入步骤 4（复核检查）；

3. **提供修改建议** — 对发现的问题给出具体修复方案；
   - 依次对每个问题提供修复建议；
   - 通过 AskUserQuestion 提供选项，阻塞等待用户选择：
     - 采纳全部建议 -> 生成修正后的代码，进入步骤 4；
     - 逐条确认 -> 逐条由用户决定采纳或忽略，全部确认后进入步骤 4；
     - 仅查看不修改 -> 不影响代码，直接进入步骤 4；

4. **复核检查** — 对照 [Review List](#review-list)，确认执行结果；
   - 判断 Review List 是否有内容：
     - 否 -> 直接进入下一步（成果输出）；
     - 是 -> 下一步；
   - 依次判断 [Review List](#review-list) 中每个检查项，是否通过：
     - 基于“复核检查示例”来显示输出内容；
   - 有任一检查失败，终止流程；
   - 全部通过后，进入下一步；

5. **成果输出** — 输出审查摘要，告知完成；
   - 输出结构化摘要表格（审查文件 / 审查项数 / 通过 / 发现问题 / 风险等级 / 已采纳建议）；
   - 告知审查完成；

## Rules

- **内容规范**
  - description 须遵循格式：使用第三人称，不超过 1024 字符；
  - SKILL.md 不超过 300 行；引用层次不超过一层；
  - 删除任何时效性信息；
- **行为规范**
  - 审查时仅输出问题摘要，不直接修改代码；
  - 所有交互环节必须使用 AskUserQuestion 工具；
  - 作用域优化标注为性能建议而非缺陷；
- **防御规范**
  - 如目标代码为空或不可读，直接报告并终止；
  - 循环依赖仅在可见 import 关系时报告；
- **验证规范**
  - Examples 必须包含复核检查示例；
  - 对话交互示例仅聚焦步骤 0~3；

## Examples

### 对话交互示例

**示例：用户请求审查循环依赖**

```markdown
用户 > 我的 AuthModule 和 UserModule 互相依赖，帮我检查
AI   > 检测到问题，触发 nestjs-7-2-scopes-circular-deps 技能
AI   > 分析代码发现 AuthModule 和 UserModule 存在循环引用...
AI   > 建议使用 forwardRef(() => UserModule) 和 forwardRef(() => AuthModule)。是否应用？
用户 > 是
```

### 复核检查示例

```markdown
AI > 进入复核检查，Review List 包含 4 个检查项，开始逐项验收：

**内容检查**
  - 🟩 审查项引用官方文档
  - 🟩 作用域建议标注为性能建议

**行为检查**
  - 🟩 使用了 AskUserQuestion

**验证检查**
  - 🟩 循环依赖在可见时报告
  - 🟩 输出摘要完整
✅ 全部通过。
```

### 成果输出示例

```markdown
| 维度 | 结果 |
| --- | --- |
| 审查文件 | src/auth/auth.module.ts |
| 审查项总数 | 5 项 |
| 通过 | 3 项 |
| 发现问题 | 2 项 |
| 风险等级 | 🟡 中 |
| 已采纳建议 | 2 条 |
```

## Review List

- **内容检查**
  - [ ] 审查项引用 NestJS 官方作用域和循环依赖文档
  - [ ] 作用域优化标注为性能建议
- **行为检查**
  - [ ] 所有交互环节使用了 AskUserQuestion
- **验证检查**
  - [ ] 循环依赖仅在可见 import 时报告
  - [ ] 输出摘要包含文件路径和审查结果

## References

- [NestJS 注入作用域文档](https://docs.nestjs.com/fundamentals/injection-scopes)
- [NestJS 循环依赖](https://docs.nestjs.com/fundamentals/circular-dependency)
- [skill-evolve 模板](../../skill-evolve/template.md)
