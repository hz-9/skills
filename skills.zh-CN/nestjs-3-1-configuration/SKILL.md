---
name: nestjs-3-1-configuration
description: 审查 NestJS 多环境配置管理实践，涵盖 @nestjs/config、自定义配置加载、验证和动态配置注入。当用户需要审查配置模块或排查环境配置问题时使用。
---

# NestJS 多环境配置管理

## Overview

当 AI 在 NestJS 项目中遇到配置管理相关代码时，自动执行以下工作：审查 ConfigModule 的配置和自定义命名空间的使用，检查环境变量加载和验证逻辑，评估动态配置注入的可靠性，识别常见配置管理问题，并提供改进建议。

## Definitions

- <a id="目标代码"></a>**目标代码**：当前对话中 NestJS 的 ConfigModule 配置、环境变量文件或自定义配置加载代码。
- <a id="ConfigModule"></a>**ConfigModule**：@nestjs/config 提供的配置模块，支持 .env 文件加载、命名空间和配置验证。
- <a id="配置命名空间"></a>**配置命名空间**：通过 ConfigModule.forRoot 的 load 选项注册的自定义配置函数，返回按领域分组的配置对象。
- <a id="是否分析完成"></a>**是否分析完成**：标记对目标代码的分析是否已得出完整结果。

## Prerequisites

- NestJS 项目环境（包含 @nestjs/config 依赖）；
- ConfigModule 配置或环境变量文件可访问；
- 了解 12-factor 应用的配置管理原则。

## Workflow

0. **前置检查** — 确保目标代码和运行环境可达；
   - 判断目标代码是否存在且可读取：
     - 是 -> 下一步；
     - 否 -> 提示用户提供目标代码或文件路径，阻塞等待用户输入；
   - 初始化全局变量 [是否分析完成](#是否分析完成)：
     - 判断代码是否完整可解析：
       - 满足 -> 初始化变量为 true；
       - 不满足 -> 初始化变量为 false；

1. **分析配置代码** — 读取并理解配置管理和环境变量加载；
   - 读取目标代码，识别以下核心要素：
     - ConfigModule.forRoot() 的配置选项（envFilePath、ignoreEnvFile、load、validationSchema）；
     - 环境变量文件（.env、.env.development、.env.production）的命名和位置；
     - 自定义配置命名空间的实现（registerAs 函数）；
     - 配置的验证方式（joi 或 zod schema 验证）；

2. **逐项审查** — 对照审查清单检查配置管理代码质量；
   - 依次判断以下审查项是否通过：
     - 是否为不同环境准备了独立的 .env 文件（开发 / 测试 / 生产）：
       - 通过 -> 记录通过，继续下一个审查项；
       - 未通过 -> 记录“建议为不同环境维护独立的 .env 文件”，继续；
     - 是否使用了配置验证 Schema（joi 或 zod 对必填字段和类型进行校验）：
       - 通过 -> 记录通过，继续下一个审查项；
       - 未通过 -> 记录“缺少配置验证 Schema，启动时无法捕获配置错误”，继续；
     - 自定义配置是否使用了 registerAs 命名空间（避免配置键全局冲突）：
       - 通过 -> 记录通过，继续下一个审查项；
       - 未通过 -> 记录“建议使用 registerAs 创建配置命名空间代替直接读取 process.env”，继续；
     - .env 文件是否被 .gitignore 包含（避免敏感信息泄漏）：
       - 提供修改建议但不阻塞 -> 继续；
     - 所有配置项是否有合理的默认值（避免因缺失环境变量导致运行时错误）：
       - 通过 -> 记录通过，继续下一个审查项；
       - 未通过 -> 记录“缺少默认值的配置项在环境变量缺失时会返回 undefined”，继续；
     - isGlobal 是否按需设置（避免全局 ConfigModule 导致隐式依赖）：
       - 通过 -> 记录通过，继续下一个审查项；
       - 未通过 -> 记录“isGlobal: true 让所有模块直接使用 ConfigService”，继续；
   - 判断是否有任何问题记录：
     - 是 -> 汇总问题列表，进入下一步；
     - 否 -> 直接进入步骤 4（复核检查）；

3. **提供修改建议** — 对发现的问题给出具体修复方案；
   - 依次对每个问题提供修复建议；
   - 通过 AskUserQuestion 提供选项，阻塞等待用户选择：
     - 采纳全部建议 -> 生成修正后的代码，进入步骤 4；
     - 逐条确认 -> 逐条由用户决定采纳或忽略，全部确认后进入步骤 4；
     - 仅查看不修改 -> 不影响代码，直接进入步骤 4；

4. **复核检查** — 对照 [Review List](#review-list)，确认执行结果；
   - 判断 Review List 是否有内容：
     - 否 -> 直接进入下一步（成果输出）；
     - 是 -> 下一步；
   - 依次判断 [Review List](#review-list) 中每个检查项，是否通过：
     - 基于“复核检查示例”来显示输出内容；
   - 有任一检查失败，终止流程；
   - 全部通过后，进入下一步；

5. **成果输出** — 输出审查摘要，告知完成；
   - 输出结构化摘要表格（审查文件 / 审查项数 / 通过 / 发现问题 / 风险等级 / 已采纳建议）；
   - 告知审查完成；

## Rules

- **内容规范**
  - description 须遵循格式：第一句说明审查内容，第二句说明触发条件，使用第三人称，不超过 1024 字符；
  - 审查项须引用 NestJS 官方配置管理文档；
  - SKILL.md 不超过 300 行；超过时拆分到 references/ 目录；
  - 引用层次不超过一层；
  - 保持术语一致，Definitions 中已定义术语在正文中通过锚点链接引用；
  - 删除任何时效性信息；

- **行为规范**
  - 审查时仅输出问题摘要和定位，不直接修改代码，除非用户通过 AskUserQuestion 明确要求；
  - 所有涉及代码修改的交互环节，必须使用 AskUserQuestion 工具；
  - isGlobal 和命名空间使用标注为架构建议而非强制；

- **防御规范**
  - 如目标代码为空或不可读，直接报告并终止；
  - .env 被 .gitignore 包含的建议仅提供提示，不阻塞流程；

- **验证规范**
  - Examples 内容必须自洽于 Rules；
  - Examples 必须包含复核检查示例；
  - 对话交互示例仅聚焦步骤 0~3（不含复核检查和成果输出）；

## Examples

### 对话交互示例

**示例：用户请求审查配置管理**

```markdown
用户 > 帮我检查项目的配置管理是否规范
AI   > 检测到用户需要 NestJS 配置管理审查，触发 nestjs-3-1-configuration 技能
AI   > 正在分析配置代码...

审查结果：
- 🟩 不同环境有独立的 .env 文件
- 🟥 缺少 joi 验证 Schema，配置错误无法在启动时捕获
- 🟩 使用 registerAs 创建了数据库和 Redis 命名空间
- 🟥 部分配置项（DB_PORT）没有默认值
- 🟩 .env 已被 .gitignore 覆盖

总评：2 个问题需要关注
用户 > 添加验证 Schema 和默认值
AI   > 请提供环境变量的类型和必填要求，由我生成配置 Schema？ 用户 > 好
```

### 复核检查示例

```markdown
AI > 进入复核检查，Review List 包含 5 个检查项，开始逐项验收：

**内容检查**
  - 🟩 审查项全部引用官方文档
  - 🟩 isGlobal 建议标注为建议

**行为检查**
  - 🟩 未直接修改用户代码
  - 🟩 使用了 AskUserQuestion

**验证检查**
  - 🟩 .env 建议已标注为提示
  - 🟩 输出摘要完整

✅ 全部通过，进入成果输出。
```

### 成果输出示例

**审查结果示例：**

```markdown
| 维度 | 结果 |
| --- | --- |
| 审查文件 | src/config/app.config.ts |
| 审查项总数 | 7 项 |
| 通过 | 5 项 |
| 发现问题 | 2 项 |
| 风险等级 | 🟡 中 |
| 已采纳建议 | 2 条 |
```

## Review List

- **内容检查**
  - [ ] 审查项全部引用 NestJS 官方配置管理文档
  - [ ] isGlobal 和命名空间标注为架构建议
- **行为检查**
  - [ ] 未直接修改用户代码（除非用户明确要求）
  - [ ] 所有交互环节使用了 AskUserQuestion
- **验证检查**
  - [ ] 目标代码为空或不可读时已正确终止
  - [ ] .env 泄漏提示不阻塞流程
  - [ ] 输出摘要包含了文件路径、审查项数、发现问题和风险等级

## References

- [NestJS 配置管理官方文档](https://docs.nestjs.com/techniques/configuration)
- [@nestjs/config 文档](https://github.com/nestjs/config)
- [skill-evolve 模板](../../skill-evolve/template.md)
