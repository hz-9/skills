---
name: nestjs-2-1-middleware
description: 审查 NestJS 中间件的实现与应用，包括函数式中间件、类中间件和全局中间件的注册与执行顺序。当用户需要审查或开发中间件逻辑时使用。
---

# NestJS 中间件应用

## Overview

当 AI 在 NestJS 项目中遇到中间件相关代码时，自动执行以下工作：审查中间件的注册方式和作用范围，检查函数式中间件和类中间件的实现规范，评估中间件执行顺序和适用场景，并提供改进建议。

## Definitions

- <a id="目标代码"></a>**目标代码**：当前对话中 NestJS 的中间件实现代码或配置代码。
- <a id="中间件类型"></a>**中间件类型**：NestJS 支持的两种中间件形式 —— 函数式中间件（Function Middleware）和类中间件（Class Middleware，实现 NestMiddleware 接口）。
- <a id="注册方式"></a>**注册方式**：中间件的三种挂载方式 —— 全局中间件（app.use）、模块级中间件（configure 方法）、控制器级中间件（@MiddlewareConsumer）。
- <a id="是否分析完成"></a>**是否分析完成**：标记对目标代码的分析是否已得出完整结果。

## Prerequisites

- NestJS 项目环境；
- 中间件代码或配置代码文件可访问；
- 理解中间件在请求处理链中的位置和用途。

## Workflow

0. **前置检查** — 确保目标代码和运行环境可达；
   - 判断目标代码是否存在且可读取：
     - 是 -> 下一步；
     - 否 -> 提示用户提供目标代码或文件路径，阻塞等待用户输入；
   - 初始化全局变量 [是否分析完成](#是否分析完成)：
     - 判断代码是否完整可解析：
       - 满足 -> 初始化变量为 true；
       - 不满足 -> 初始化变量为 false；

1. **分析中间件代码** — 读取并理解中间件实现和配置；
   - 读取目标代码，识别以下核心要素：
     - 中间件类型（函数式 vs 类中间件）；
     - 中间件应用配置（全局 app.use / 模块 configure / 控制器级）；
     - next() 调用是否在所有分支中被正确处理；

2. **逐项审查** — 对照审查清单检查中间件代码质量；
   - 依次判断以下审查项是否通过：
     - 中间件类型选择是否合理（类中间件支持 DI 注入，函数式中间件适合无依赖场景）：
       - 通过 -> 记录通过，继续下一个审查项；
       - 未通过 -> 记录“如需依赖注入应使用类中间件”，继续；
     - 所有分支逻辑是否都调用了 next()（避免请求被挂起）：
       - 通过 -> 记录通过，继续下一个审查项；
       - 未通过 -> 记录“存在分支未调用 next()，可能导致请求超时”，继续；
     - 全局中间件是否通过 app.use() 注册（非 Nest 模块管理范围内）：
       - 通过 -> 记录通过，继续下一个审查项；
       - 未通过 -> 记录“全局中间件推荐在 main.ts 中使用 app.use()”，继续；
     - exclude() 路径排除是否正确（避免敏感路由被中间件拦截）：
       - 通过 -> 记录通过，继续下一个审查项；
       - 未通过 -> 记录“排除路径配置有误，请检查正则或路径字符串”，继续；
     - 中间件是否包含异步操作且未正确处理错误（未捕获的异步异常）：
       - 通过 -> 记录通过，继续下一个审查项；
       - 未通过 -> 记录“中间件中的异步操作需使用 try/catch 或 catchError”，继续；
   - 判断是否有任何问题记录：
     - 是 -> 汇总问题列表，进入下一步；
     - 否 -> 直接进入步骤 4（复核检查）；

3. **提供修改建议** — 对发现的问题给出具体修复方案；
   - 依次对每个问题提供修复建议，包括正反对比示例；
   - 通过 AskUserQuestion 提供选项，阻塞等待用户选择：
     - 采纳全部建议 -> 生成修正后的代码，进入步骤 4；
     - 逐条确认 -> 逐条由用户决定采纳或忽略，全部确认后进入步骤 4；
     - 仅查看不修改 -> 不影响代码，直接进入步骤 4；

4. **复核检查** — 对照 [Review List](#review-list)，确认执行结果；
   - 判断 Review List 是否有内容：
     - 否 -> 直接进入下一步（成果输出）；
     - 是 -> 下一步；
   - 依次判断 [Review List](#review-list) 中每个检查项，是否通过：
     - 基于“复核检查示例”来显示输出内容；
   - 有任一检查失败，终止流程；
   - 全部通过后，进入下一步；

5. **成果输出** — 输出审查摘要，告知完成；
   - 输出结构化摘要表格（审查文件 / 审查项数 / 通过 / 发现问题 / 风险等级 / 已采纳建议）；
   - 告知审查完成；

## Rules

- **内容规范**
  - description 须遵循格式：第一句说明审查内容，第二句说明触发条件，使用第三人称，不超过 1024 字符；
  - 审查项须引用 NestJS 官方关于中间件的文档；
  - SKILL.md 不超过 300 行；超过时拆分到 references/ 目录；
  - 引用层次不超过一层；
  - 保持术语一致，Definitions 中已定义术语在正文中通过锚点链接引用；
  - 删除任何时效性信息；

- **行为规范**
  - 审查时仅输出问题摘要和定位，不直接修改代码，除非用户通过 AskUserQuestion 明确要求；
  - 所有涉及代码修改的交互环节，必须使用 AskUserQuestion 工具；
  - 中间件类型选择的建议标注为建议而非强制；

- **防御规范**
  - 如目标代码为空或不可读，直接报告并终止；
  - 排除路径正则表达式复杂时标注为需手动确认正确性；

- **验证规范**
  - Examples 内容必须自洽于 Rules；
  - Examples 必须包含复核检查示例；
  - 对话交互示例仅聚焦步骤 0~3（不含复核检查和成果输出）；

## Examples

### 对话交互示例

**示例：用户请求审查中间件实现**

```markdown
用户 > 帮我检查这段中间件代码
AI   > 检测到用户需要 NestJS 中间件审查，触发 nestjs-2-1-middleware 技能
AI   > 正在分析中间件代码...

审查结果：
- 🟩 使用类中间件实现 NestMiddleware 接口，支持 DI 注入
- 🟥 错误处理分支未调用 next()，可能挂起请求
- 🟩 模块级 configure() 中配置了 forRoutes 和 exclude
- 🟩 exclude 路径正确排除了健康检查端点

总评：1 个问题需要关注
用户 > 请修复 next() 问题
AI   > 错误分支需要添加 return next(error)。是否应用修改？
用户 > 是
```

### 复核检查示例

```markdown
AI > 进入复核检查，Review List 包含 5 个检查项，开始逐项验收：

**内容检查**
  - 🟩 审查项全部引用官方文档
  - 🟩 中间件类型建议标注为建议

**行为检查**
  - 🟩 未直接修改用户代码
  - 🟩 使用了 AskUserQuestion

**验证检查**
  - 🟩 排除路径正则已标注需手动确认
  - 🟩 输出摘要完整

✅ 全部通过，进入成果输出。
```

### 成果输出示例

**审查结果示例：**

```markdown
| 维度 | 结果 |
| --- | --- |
| 审查文件 | src/common/middleware/auth.middleware.ts |
| 审查项总数 | 6 项 |
| 通过 | 5 项 |
| 发现问题 | 1 项 |
| 风险等级 | 🟡 中 |
| 已采纳建议 | 1 条 |
```

## Review List

- **内容检查**
  - [ ] 审查项全部引用 NestJS 官方中间件文档
  - [ ] 中间件类型建议标注为建议而非强制
- **行为检查**
  - [ ] 未直接修改用户代码（除非用户明确要求）
  - [ ] 所有交互环节使用了 AskUserQuestion
- **验证检查**
  - [ ] 目标代码为空或不可读时已正确终止
  - [ ] 排除路径正则标注为需手动确认
  - [ ] 输出摘要包含了文件路径、审查项数、发现问题和风险等级

## References

- [NestJS 中间件官方文档](https://docs.nestjs.com/middleware)
- [skill-evolve 模板](../../skill-evolve/template.md)
